## 1. Backend Setup

- [x] 1.1 Create `backend/requirements.txt` with fastapi, uvicorn, pydantic dependencies
- [x] 1.2 Create `backend/models.py` with stub Pydantic models for Order and Product
- [x] 1.3 Create `backend/main.py` with minimal FastAPI app (health endpoint + stub API routes)

## 2. Frontend Setup

- [x] 2.1 Create `frontend/index.html` with basic HTML structure (Tailwind CDN, minimal layout)
- [x] 2.2 Create `frontend/js/app.js` with empty/minimal JavaScript
- [x] 2.3 Create `frontend/css/style.css` with minimal custom styles

## 3. Data Files

- [x] 3.1 Create `data/orders.json` with empty array `[]`
- [x] 3.2 Create `data/products.json` with empty array `[]`

## 4. Docker Configuration Fix

- [x] 4.1 Fix healthcheck in `docker-compose.yml` (use port 8000 instead of 9000)
